if [ -z "$EDITOR" ]; then
    [ -n "${DEBUG}" ] && echo -e $BLUE"(!)"$GREEN" no preferred EDITOR set !"$NO_COLOR
else
    [ -n "${DEBUG}" ] && echo -e $GREEN"+++ preferred EDITOR is \`$EDITOR\`"$NO_COLOR
fi
